# Music Tracker - Environment Configuration
# This file contains example environment variables for the Music Tracker application
# Copy this to .env and fill in your actual credentials

# Spotify API Credentials (Required)
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret
SPOTIFY_REFRESH_TOKEN=your_spotify_refresh_token

# ListenBrainz/Navidrome Configuration (Optional)
# Required for ingesting play history from Navidrome via ListenBrainz
LB_API_ROOT=https://api.listenbrainz.org/1
LB_USER=your_listenbrainz_username
LB_TOKEN=your_listenbrainz_token
TEST_MODE=false  # Set to true to only fetch last 24 hours

# OpenWeather API Credentials (Optional)
OPENWEATHER_API_KEY=your_openweather_api_key

# n8n Configuration (Required for workflow orchestration)
N8N_HOST=localhost
TIMEZONE=UTC

# Application Configuration
ENVIRONMENT=development  # or production
DUCKDB_PATH=//home/runner/workspace/data/music_tracker.duckdb
LOG_LEVEL=INFO

# Metabase Configuration (Required for reporting)
MB_DB_TYPE=h2
MB_DB_FILE=/home/metabase.db
METABASE_PORT=3000
METABASE_API_KEY=your_metabase_api_key

# Synology NAS Configuration
# For Synology deployment, use the following settings:
# - Access via http://<synology-ip>:3000 for Metabase
# - Access via http://<synology-ip>:5678 for n8n UI
# - Map volumes to /volume1/docker/music-tracker for persistent storage

# Memory Optimization (for 2GB RAM Synology NAS)
# These settings are optimized for Synology NAS with limited memory
MAX_MEMORY_USAGE=1300m  # Keep under 2GB total (700MB freed from Prefect removal)
N8N_MEMORY=300m
METABASE_MEMORY=512m
PIPELINE_MEMORY=400m
