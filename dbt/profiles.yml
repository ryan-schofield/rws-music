music-tracker:
   target: dev
   outputs:
     dev:
       type: duckdb
       path: "{{ env_var('DUCKDB_PATH', '../data/music_tracker.duckdb') }}"
       threads: 1
       extensions:
         - httpfs
         - parquet
       settings:
         memory_limit: 2GB
         threads: 1

     local:
       type: duckdb
       path: "../data/music_tracker.duckdb"
       threads: 4
       extensions:
         - httpfs
         - parquet
       settings:
         memory_limit: 4GB
         threads: 4
