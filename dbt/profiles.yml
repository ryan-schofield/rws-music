music-tracker:
   target: dev
   outputs:
     dev:
       type: duckdb
       path: "{{ env_var('DUCKDB_PATH', '/home/runner/workspace/data/music_tracker.duckdb') }}"
       threads: 1
       settings:
         memory_limit: 256MB
         threads: 1
         max_memory: 256MB

     local:
       type: duckdb
       path: "../data/music_tracker.duckdb"
       threads: 4
       extensions:
         - httpfs
         - parquet
       settings:
         memory_limit: 4GB
         threads: 4
